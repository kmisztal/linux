1. Wyświetl zawartość pliku `/etc/hostname`, a następnie dopisz do niego bieżącą datę w formacie `YYYY-MM-DD`.
2. Wyświetl wszystkie linie z pliku `/etc/passwd`, które nie zawierają słowa `nologin`.
3. Policz, ile plików w katalogu `/bin` zawiera literę `a` w nazwie.
4. Z pliku `/etc/passwd` wyświetl tylko nazwy użytkowników, posortowane alfabetycznie, bez duplikatów.
5. Wyświetl 5 największych plików w katalogu `/var/log` wraz z ich rozmiarami w czytelnym formacie.
6. Policz, ile procesów działa aktualnie dla każdego użytkownika i posortuj wynik malejąco po liczbie procesów.
7. Wypisz 10 najczęściej używanych komend przez użytkownika na podstawie jego historii (`~/.bash_history`).
8. Z pliku `/etc/passwd` utwórz plik `uids.txt`, w którym znajdą się tylko pola `login:uid` posortowane rosnąco po `UID`.
9. Znajdź wszystkie pliki w katalogu `/etc`, które zostały zmodyfikowane w ciągu ostatnich 7 dni, i zapisz ich pełne ścieżki do pliku `recent_etc.txt`.
10. Dla każdego użytkownika z pliku `/etc/passwd` wypisz jego nazwę oraz liczbę procesów, które aktualnie uruchomił.
